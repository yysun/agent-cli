# Pending Dots Waiting Indicator

## Summary

- Changed verbose CLI stream handling so three dots resume after continuation diagnostics while the turn is still waiting for assistant text.
- Kept natural-stop verbose model diagnostics from restarting the pending indicator.
- Preserved the existing non-verbose tool-continuation behavior and `--stream-off` no-dot behavior.
- Cleared pending dots before verbose assistant-text spacing so resumed dots do not leak onto prior lines.
- Added focused unit coverage for verbose tool diagnostics, verbose model continuation, and natural-stop completion.

## Verification

- `npm run build`
- `npx vitest run tests/unit/agent-cli.test.js`
- `npm run test:unit`
- `npm run check`
- CR pass: no blocking code review findings after diff inspection.
- VR pass: implementation, tests, and RPD docs match the waiting-indicator requirement.

## Notes

- No E2E spec was added; this is terminal rendering behavior covered by mocked runtime unit tests.
- `bin/agent-cli.js` was regenerated by the build because it is the checked CLI bundle.
