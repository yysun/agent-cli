# Done: global-skills-env

## Summary

- Added `AGENT_CLI_GLOBAL_SKILLS` as the opt-in switch for home-directory skill loading.
- Kept workspace skills from `.agent-world/skills` always enabled.
- Added global discovery from both `~/.agent-world/skills` and `~/.agents/skills` when the switch is enabled.
- Preserved workspace precedence over global skill ids.
- Allowed the new setting in cwd `.env` and generated `.env.example`.
- Updated README with the local/global skill-loading contract.

## Verification

- `npm run build:core`
- `npx vitest run tests/unit/agent-files.test.js tests/unit/paths.test.js tests/unit/agent-cli.test.js`
- `npm run build`
- `npm run test:unit`
- `npm run check`
- CR passed with no blocking findings.

## Notes

- No E2E spec was added; this is configuration-controlled inventory behavior covered by unit tests.
- `bin/agent-cli.js` was regenerated by `npm run build` because the CLI bundle is checked in.
